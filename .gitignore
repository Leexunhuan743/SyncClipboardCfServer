node_modules/
.wrangler/
dist/
dev.vars
*.log
.DS_Store
.dev.vars

# 防误提交：wrangler r2 object put 语法错误会在项目根创建这些路径
/history/
/file/

# 审计工作区（含本地绝对路径与调查报告），仅本地保留
.audits/

# 视觉回归截图输出（test/manual/shoot.mjs）
.shots/
# 同一用途的带后缀目录（`.shots-v1-final` 这类按轮次命名的工作目录）：
# 它们同样只是证据工件，不进仓库。加这条是为了让 `git status` 干净，
# 也免得有人用 `git add -A` 把几十张 PNG 卷进提交（ADR D11 明确禁止 `-A`）。
.shots-*/
