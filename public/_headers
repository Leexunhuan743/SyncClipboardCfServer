# Cloudflare Workers 静态资源的响应头配置（`[assets]` 目录下的特殊文件，不会被当作资源发布）。
#
# 为什么需要它：Worker 只把静态资源转回去、自己不设头，所以这批文件的响应头只能在这里声明。两件事：
#   1. **安全头**：整站同源、私有实例，故给最严的一套——脚本/样式只许本站、不许被嵌入、
#      不开 inline（首帧主题脚本外置为 `/ui_v1/js/theme-init.js`、`/ui_v2/js/theme-init.js` 正是为了这条）；
#   2. **缓存**：代码资源没有内容指纹，每次导航都须重验证，允许用 ETag 返回 304。
#      禁止直接复用旧代码：新 HTML 搭配旧模块会出现缺失功能和重复标题，已在本轮实测。
#
# ⚠️ **下面每一行的路径都是按挂载点写死的**：改目录名 / 加挂载点必须同步这里。
# 2026-09-19 的 `ui_old` → `ui_v1` / `ui` → `ui_v2` 改名就漏改过它一次 —— 规则全留在
# `/ui_old/*`（目录已不存在），于是两版前端**同时**退回平台默认的 `max-age=0`，
# 而 `docs/frontend-checklist.md` 还把这件事记成"已完成"。这条漂移能成立，是因为当时
# **没有任何守卫读这个文件**（旧结构的三个挂载点与这个文件的路径列表是两份独立的事实）。
# 现在由 `test/ui-guard.test.ts` 的两条结构判据盯着：规则路径必须真实存在、
# 三个挂载点的 js/css 必须各有一条禁令。见 `docs/progress.md` §90。

/*
  Content-Security-Policy: default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data:; font-src 'self'; connect-src 'self' wss: ws:; manifest-src 'self'; base-uri 'none'; form-action 'self'; frame-ancestors 'none'; object-src 'none'
  X-Content-Type-Options: nosniff
  Referrer-Policy: same-origin
  X-Frame-Options: DENY
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=()
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin

# ── 无指纹的 JS/CSS 必须重验证；引入构建指纹后才可改成长缓存 ──
# `_headers` 不支持一条规则覆盖多个前缀，故三个挂载点各写一份。
# `/ui/` 现在只剩一层跳转壳，但它的 `js/redirect-hash.js` 同样是代码资源 ⇒ 保留 `/ui/js/*`。
/ui/js/*
  Cache-Control: public, no-cache, must-revalidate

/ui_v1/js/*
  Cache-Control: public, no-cache, must-revalidate

/ui_v1/css/*
  Cache-Control: public, no-cache, must-revalidate

/ui_v2/js/*
  Cache-Control: public, no-cache, must-revalidate

/ui_v2/css/*
  Cache-Control: public, no-cache, must-revalidate

# ── 图标与 manifest：改动极少，可以长一些 ──
# 注意 `/ui/` 下**没有**这些文件（跳转壳不含图标），故不给它写图标规则。
/ui_v1/favicon.svg
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v1/favicon-32.png
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v1/apple-touch-icon.png
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v1/manifest.webmanifest
  Cache-Control: public, max-age=3600

/ui_v2/favicon.svg
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v2/favicon-32.png
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v2/apple-touch-icon.png
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/ui_v2/manifest.webmanifest
  Cache-Control: public, max-age=3600

/robots.txt
  Cache-Control: public, max-age=3600
